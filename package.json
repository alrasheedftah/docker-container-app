{
  "name": "docker-app-custom",
  "version": "1.0.0",
  "description": "just to implements steps of docker ",
  "main": "index.js",
  "scripts": {
    "start":"node src/app.js",
    "watch" : "nodemon src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "ana"
  ],
  "author": "ana",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
